Pathfinder single-file mobile v2

Only one file is required:
- index.html

This package is designed to avoid GitHub Pages multi-file cache mismatch.
Upload index.html to your repo root and overwrite the old one.
